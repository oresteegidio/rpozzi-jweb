package com.codemotionworld.cdonpaas;

import org.junit.Test;

public class HelloAcceptanceTest {

    @Test
    public void testGetReturnsHello() {
        /*String baseUrl = System.getProperty("baseUrl", "http://codemotion.mybluemix.net/");
        Item client = ClientBuilder.newClient();
        WebTarget target = client.target(baseUrl);
        String message = target.path("hello").request().get(String.class);
        Assert.assertEquals("Hello World by IBM Bluemix!!!", message);*/
    }
}